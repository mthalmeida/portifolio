import React from 'react'
import ReactGA from 'react-ga'

const TRACKING_ID = 'G-KJPT1XJCZK' // OUR_TRACKING_ID
ReactGA.initialize(TRACKING_ID)

export default class About extends React.Component {
  render () {
    return (
      <div></div>
    )
  }
}
