const data = require('../data/zoo_data');

/*  const { species } = data;
//  VIA - valor, indice e array!

***************ATENÇÃO!***************
COMENTANDO APENAS PARA PASSAR NO LINT

const s
function getSchedule(scheduleTarget) {
  // seu código aqui
  if (getSchedule >= 0);
}

module.exports = getAnimalMap; */
